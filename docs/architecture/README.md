# Arquitectura

Esta carpeta documentará la organización del workspace Angular, los límites entre `core`,
`features` y `shared`, la composición standalone y la estrategia de rutas. En la Fase 0 solo se
registra la base; las decisiones funcionales se incorporarán cuando exista implementación real.
